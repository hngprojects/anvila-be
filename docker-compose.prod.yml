services:
  app_prod:
    image: anchor-python-bp-prod:latest
    command: ["sh", "-c", "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 7001 --workers 4"]
    container_name: app_prod
    env_file:
      - .env
    networks:
      - hng-network
    restart: unless-stopped
    ports:
      - "7007:7001"
    working_dir: /app
    depends_on:
      prod_db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7001/probe"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "2.0"

  prod_db:
    image: postgres:14.12
    container_name: prod_db
    restart: unless-stopped
    volumes:
      - prod_db:/var/lib/postgresql/data
    env_file:
      - .env
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD?Variable not set}
      - POSTGRES_USER=${DB_USER?Variable not set}
      - POSTGRES_DB=${DB_NAME?Variable not set}
    networks:
      - hng-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"

volumes:
  prod_db:

networks:
  hng-network:
    driver: bridge