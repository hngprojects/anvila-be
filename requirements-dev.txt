# Development / CI tooling. Not installed on deploy hosts.
#
# Phase 2 introduces this file with ruff + pytest-cov.
# Phase 5/6 will move the rest of the dev tooling (pytest, black, flake8, etc.)
# from requirements.txt to this file.
#
# Pins are exact (==) for CI reproducibility.

ruff==0.15.12
pytest-cov==7.1.0
